#!/usr/bin/env sh

# We sed in SRC_DIR because Solaris `make` doesn't set CURDIR
SED="sed -e s|@SRC_DIR@|"`pwd`"/src|"

if [ $(uname) = Darwin ]; then
    MK_FILE="Makebuild"
else
    MK_FILE="Makebuild"
fi

${SED} -e "s|@MK_FILE@|$MK_FILE|" src/Makevars.in > src/Makevars
